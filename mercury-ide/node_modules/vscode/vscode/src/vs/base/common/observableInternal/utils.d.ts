import { IObservable, ITransaction } from './base.js';

interface IObservableSignal<TChange> extends IObservable<void, TChange> {
    trigger(tx: ITransaction | undefined, change: TChange): void;
}

export type { IObservableSignal };
